# SOCKS5 Installer (3proxy)

This repo installs a SOCKS5 proxy using 3proxy (build from source), aiming to be compatible with Ubuntu, CentOS/RHEL-like, and lightweight cloud images.

## One-liner install (recommended)

### Safe default (localhost only)
```bash
curl -fsSL https://raw.githubusercontent.com/DavidYordan/socks5-installer/main/install.sh | sudo bash
